/*
@AUTHOR : SAGAR RATHI
@DATE : 18 NOV 2022
@GOAL : TO DISPLAY GREETINGS 
*/

#include<Windows.h>

int	WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nShowCmd)
{

	MessageBox(NULL, TEXT("Gretings !"), TEXT("MASTER CLASS IN C BATCH 3 "), MB_OK);
	return(0);
}